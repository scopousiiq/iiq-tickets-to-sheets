{
  "version": "1.8.5",
  "released": "2026-09-18",
  "releaseUrl": "https://github.com/scopousiiq/iiq-tickets-to-sheets/releases/latest",
  "message": "SLA columns now fill in reliably on districts of any size. SLA data is fetched in smaller chunks, so the requests no longer time out and return an error the way they did on larger or slower districts \u2014 and if one chunk does fail, the rest of the SLA data still loads instead of the whole batch coming back blank. TICKET_BATCH_SIZE in Config is now just a pagination setting and is safe at any value."
}
